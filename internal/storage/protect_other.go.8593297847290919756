//go:build !windows

package storage

func encrypt(data []byte) ([]byte, error) {
	return append([]byte(nil), data...), nil
}

func decrypt(data []byte) ([]byte, error) {
	return append([]byte(nil), data...), nil
}

